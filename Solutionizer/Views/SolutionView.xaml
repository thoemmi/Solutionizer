<UserControl x:Class="Solutionizer.Views.SolutionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:Helper="clr-namespace:Solutionizer.Helper"
             xmlns:ViewModels="clr-namespace:Solutionizer.ViewModels"
             xmlns:Converters="clr-namespace:Solutionizer.Converters"
             d:DataContext="{d:DesignInstance ViewModels:SolutionViewModel}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
    <Grid.Resources>
      <Converters:FilePathToImageSourceConverter x:Key="FilePathToImageSource"/>
      <DataTemplate x:Key="NameWithImage" DataType="{x:Type ViewModels:Project}">
        <StackPanel Orientation="Horizontal">
          <Image Height="16" Width="16" Source="{Binding Path=Filepath, Converter={StaticResource FilePathToImageSource}}" Margin="0 0 4 0"/>
          <TextBlock Text="{Binding Name}" />
        </StackPanel>
      </DataTemplate>
    </Grid.Resources>
      
    <Grid.RowDefinitions>
      <RowDefinition Height="auto" />
      <RowDefinition />
      <RowDefinition Height="auto" />
      <RowDefinition Height="auto" />
      <RowDefinition />
      <RowDefinition Height="auto"/>
    </Grid.RowDefinitions>

    <TextBlock Margin="0 2">Selected projects:</TextBlock>
        
    <ListView 
      Grid.Row="1"
      ItemsSource="{Binding Projects}" 
      Helper:GridViewSort.AutoSort="true"
      Helper:GridViewSort.ShowSortGlyph="true"
      Helper:DragDropHelper.IsDropTarget="True" Helper:DragDropHelper.DropCommand="{Binding DropCommand}" 
      Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}">
      <ListView.View>
        <GridView>
          <GridViewColumn Header="Name" CellTemplate="{StaticResource NameWithImage}" Helper:GridViewSort.PropertyName="Name"/>
          <GridViewColumn Header="Assembly" DisplayMemberBinding="{Binding AssemblyName}" Helper:GridViewSort.PropertyName="AssemblyName"/>
          <GridViewColumn Header="Path" DisplayMemberBinding="{Binding Filepath}" Helper:GridViewSort.PropertyName="Path"/>
        </GridView>
      </ListView.View>
    </ListView>

    <GridSplitter Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Stretch" ShowsPreview="true" Height="6" Background="Transparent"/>

    <TextBlock Margin="0 2" Grid.Row="3">Referenced projects:</TextBlock>
        
    <ListView 
      Grid.Row="4" 
      ItemsSource="{Binding ReferencedProjects}" 
        Helper:GridViewSort.AutoSort="true"
        Helper:GridViewSort.ShowSortGlyph="true"
      Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}">
      <ListView.View>
        <GridView>
          <GridViewColumn Header="Name" CellTemplate="{StaticResource NameWithImage}" Helper:GridViewSort.PropertyName="Name"/>
          <GridViewColumn Header="Assembly" DisplayMemberBinding="{Binding AssemblyName}" Helper:GridViewSort.PropertyName="AssemblyName"/>
          <GridViewColumn Header="Path" DisplayMemberBinding="{Binding Filepath}" Helper:GridViewSort.PropertyName="Path"/>
        </GridView>
      </ListView.View>
    </ListView>

    <StackPanel Grid.Row="5" Orientation="Horizontal">
      <TextBlock Text="Projects: "/>
      <TextBlock Text="{Binding Projects.Count}"/>
      <TextBlock Text=" - References: "/>
      <TextBlock Text="{Binding ReferencedProjects.Count}"/>
    </StackPanel>
  </Grid>
</UserControl>
